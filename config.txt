armstub=RPI_EFI.fd
device_tree_address=0x1f0000
device_tree_end=0x210000

# Linux GPU acceleration in Device Tree mode requires the matching kernel's
# board DTB and overlays directory. See README.md before enabling:
#dtoverlay=vc4-kms-v3d

# Force 32 bpp framebuffer allocation.
framebuffer_depth=32

# Disable compensation for displays with overscan.
disable_overscan=1

# Force maximum USB power regardless of the power supply.
usb_max_current_enable=1

# Force maximum CPU speed.
force_turbo=1
